#################################
# VirtualService
#################################
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: be-ap-vs-mesh
  namespace: lra-local
spec:
  hosts:
  - be-ap-svc.lra-local.svc.cluster.local
  http:
  - route:
    - destination:
        host: be-ap-svc.lra-local.svc.cluster.local
        port:
          number: 9011
