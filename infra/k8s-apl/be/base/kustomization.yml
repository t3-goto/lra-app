#################################
# Kustomization (base)
#################################
resources:
- deployment.yml
- destination-rule.yml
- service.yml
- virtual-service-mesh.yml

generatorOptions:
  disableNameSuffixHash: true
